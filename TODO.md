# TODO

- Add CRUD API endpoints for `placement_metrics`.
- Add repository helpers for reading/writing `placement_metrics` rows.
- Decide route placement (`/metrics` vs `/admin/metrics`) and auth requirements.
